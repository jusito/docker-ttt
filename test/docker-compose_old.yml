volumes:
  TTTDev:

services:
  ttt:
    build: .
    ports:
      - 27015:27015/tcp
      - 27015:27015/udp
    environment:
      INSTALL_CSS: "true"
      INSTALL_HL2: "true"
      SERVER_DEFAULT_MAP: ttt_byperbasedome
      SERVER_PORT: 27015
      SERVER_NAME: "Passiv Terror"
      SERVER_PASSWORD: Terror
      SERVER_VOICE_ENABLE: 1
      SERVER_MAX_PLAYERS: 20
      SERVER_RCON_PASSWORD: BrauchIchNicht
      WORKSHOP_COLLECTION_ID: 3167400140
    volumes:
      - TTTDev:/home/steam/serverfiles
